# Parsing RSS feeds (kept for future use)
feedparser>=6.0.0

# Date parsing for RSS feeds
python-dateutil>=2.8.0

# HTTP requests for scrapers and ntfy
requests>=2.28.0

# Browser impersonation for Daft.ie (bypasses bot detection)
curl_cffi>=0.5.0

# HTML parsing for MyHome.ie scraper
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Loading environment variables
python-dotenv>=1.0.0

# Parsing config.yaml
pyyaml>=6.0

# Running periodic scraping jobs
schedule>=1.2.0

# Tiny web server for email approval buttons
flask>=3.0.0
